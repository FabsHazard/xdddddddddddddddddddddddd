name: Upload artifact
uses: actions/upload-artifact@v3
with:
  name: my-artifact
  path: path/to/artifact
on:
  push:
    branches: [ main ]  # Triggers the workflow on push to the `main` branch
  workflow_dispatch:  # Allows manual execution from the GitHub Actions tab

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4  # Updated to latest stable version

      - name: Cache APT packages
        uses: awalsh128/cache-apt-pkgs-action@v1.4.3  # Upgraded version
        with:
          packages: git curl
          version: 1.0

      - name: Install dependencies
        run: npm ci  # Ensures a clean installation of dependencies

      - name: Build site
        run: npm run build  # Builds the site

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1  # Ensures compatibility with GitHub Pages
        with:
          path: ./dist  # Make sure this is the correct output directory

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2  # Updated to latest version
